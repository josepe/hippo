    function out=removelines(in,freqs,Q)
         import hp.*
            out=in;
            if nargin<3
                Q=45;
            end
            if ~isempty(freqs)
            wo=freqs(1);
            bw=wo/Q;
            [b,a] = iirnotch(wo,bw);
            
            
                r=removelines(in,
                freqs(1)=[];
            
        end