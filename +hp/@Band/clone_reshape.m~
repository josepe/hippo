         function  out=clone_reshape(in,winlength)
         %winlength in seconds
            import hp.*
            out=in;
            len=length(out.data);
            samplesperwin=floor(winlength*out.sampleFreq);
            nwins=mod(len,samplesperwin);
            usedata=out.wave(1:nwins*samplesperwin);
            usetime=
            
            totsecs=len/out.sampleFreq;
            nwins=floor(totsecs/winlength);
            nsamples=nwins*winlength
            m=winlength*out.sampleFreq;
            le=floor(len
            
            
            out.data=Band.subsample(in,newfreq);
            leng=length(out.data);
            out.tvector=Band.t_vector(newfreq,leng);
            out.nrSamples=leng;
            out.sampleFreq=newfreq;
        end